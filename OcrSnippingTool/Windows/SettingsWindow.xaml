<Window x:Class="OcrSnippingTool.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR Snipping Tool — Settings"
        Width="400" Height="320"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Background="#F3F3F3">

    <Window.Resources>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,4"/>

        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Hotkey -->
        <TextBlock Grid.Row="0" Text="HOTKEY" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="HotkeyBox"
                     Grid.Column="0"
                     IsReadOnly="True"
                     FontSize="13"
                     Padding="8,5"
                     VerticalContentAlignment="Center"
                     Background="White"
                     BorderBrush="#CCC"
                     PreviewKeyDown="OnHotkeyBoxKeyDown"/>
            <Button x:Name="RecordBtn"
                    Grid.Column="1"
                    Content="Record"
                    Margin="6,0,0,0"
                    Click="OnRecordClick"/>
            <Button Grid.Column="2"
                    Content="Reset"
                    Margin="4,0,0,0"
                    Click="OnResetHotkey"/>
        </Grid>

        <!-- OCR Language -->
        <TextBlock Grid.Row="2" Text="OCR LANGUAGE" Style="{StaticResource SectionLabel}"/>
        <ComboBox x:Name="LanguageCombo"
                  Grid.Row="3"
                  FontSize="13"
                  Padding="8,4"
                  Height="32"/>

        <!-- Options -->
        <TextBlock Grid.Row="4" Text="OPTIONS" Style="{StaticResource SectionLabel}"/>
        <StackPanel Grid.Row="5">
            <CheckBox x:Name="AutoStartCheck" Content="Start with Windows" FontSize="13" Margin="0,0,0,6"/>
            <CheckBox x:Name="NotifyCheck"    Content="Show notification after copy" FontSize="13"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancel" Click="OnCancelClick" Margin="0,0,8,0"/>
            <Button x:Name="SaveBtn"
                    Content="Save"
                    Click="OnSaveClick"
                    Background="#1976D2"
                    Foreground="White"
                    BorderBrush="Transparent"/>
        </StackPanel>

    </Grid>
</Window>
